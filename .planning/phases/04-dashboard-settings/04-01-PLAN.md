---
phase: 04-dashboard-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/dashboard/dashboard.module.ts
  - apps/api/src/modules/dashboard/dashboard.controller.ts
  - apps/api/src/modules/dashboard/dashboard.service.ts
  - apps/api/src/app.module.ts
  - apps/web/lib/types/dashboard.ts
  - apps/web/app/dashboard/page.tsx
  - apps/web/app/dashboard/layout.tsx
  - apps/web/app/dashboard/components/kpi-cards.tsx
  - apps/web/app/dashboard/components/conversion-funnel.tsx
  - apps/web/app/dashboard/components/activity-timeline.tsx
  - apps/web/app/dashboard/components/todays-agenda.tsx
  - apps/web/app/dashboard/components/agent-health.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-08, DASH-09]

must_haves:
  truths:
    - "Dashboard loads with live KPI cards (leads today, appointments today, conversion rate, revenue pipeline) from real backend data"
    - "Conversion funnel chart shows last 30 days of stage progression"
    - "Activity timeline shows recent agent actions, bookings, follow-ups"
    - "Today's agenda lists appointments with confirmation status"
    - "Agent health indicator shows connected status, messages/hour, errors, latency"
    - "Dashboard is usable on mobile without horizontal scroll"
    - "Sidebar has navigation links for all Phase 4 sections with clinic selector"
  artifacts:
    - path: "apps/api/src/modules/dashboard/dashboard.service.ts"
      provides: "KPI aggregation queries, funnel data, activity feed, agenda, agent health"
      exports: ["DashboardService"]
    - path: "apps/web/app/dashboard/page.tsx"
      provides: "Dashboard page with all KPI components wired to real data"
      min_lines: 40
    - path: "apps/web/app/dashboard/layout.tsx"
      provides: "Updated sidebar with full navigation and clinic selector"
      min_lines: 80
  key_links:
    - from: "apps/web/app/dashboard/page.tsx"
      to: "/api/dashboard/*"
      via: "apiClient.get calls"
      pattern: "apiClient\\.get.*dashboard"
    - from: "apps/api/src/modules/dashboard/dashboard.service.ts"
      to: "prisma"
      via: "Prisma queries for KPIs"
      pattern: "prisma\\.(lead|appointment|conversation)"
---

<objective>
Build the main dashboard page with live KPI cards, conversion funnel chart, activity timeline, today's agenda, and agent health indicator. Update the sidebar layout with full navigation for all Phase 4 sections and clinic selector dropdown.

Purpose: Give clinic managers a real-time overview of their clinic's performance and agent status.
Output: Working dashboard at /dashboard with live data from backend APIs, mobile-responsive layout, updated sidebar navigation.
</objective>

<execution_context>
@/Users/softsales/.claude/get-shit-done/workflows/execute-plan.md
@/Users/softsales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-crm-handoff-scheduling/03-01-SUMMARY.md

<interfaces>
<!-- Existing API client for frontend data fetching -->
From apps/web/lib/api.ts:
```typescript
export const apiClient = {
  get<T>(path: string, options?: RequestInit): Promise<T>,
  post<T>(path: string, body?: unknown, options?: RequestInit): Promise<T>,
  patch<T>(path: string, body?: unknown, options?: RequestInit): Promise<T>,
  delete<T>(path: string, options?: RequestInit): Promise<T>,
};
```

From apps/web/lib/auth-client.ts:
```typescript
export function useSession(): { data: { user: { name: string; email: string } } | null; isPending: boolean };
export const authClient: { signOut(): Promise<void>; organization: { list(): Promise<{ id: string; name: string }[]>; setActive(params: { organizationId: string }): Promise<void> } };
```

Existing Prisma models (from schema.prisma):
- Lead: id, organizationId, name, phone, email, procedureInterest, stage, source, score, tags, bantBudget, bantAuthority, bantNeed, bantTimeline, createdAt, updatedAt
- Appointment: id, organizationId, leadId, professionalName, procedureName, startTime, endTime, status, calendarEventId, createdAt
- Conversation: id, organizationId, remoteJid, isHandoff, lastMessageAt, messageCount, createdAt
- WhatsappInstance: id, organizationId, instanceName, status, connectedAt
- LeadTimeline: id, leadId, eventType, fromStage, toStage, metadata, createdAt

Existing NestJS API patterns:
- Controllers use @Controller("route"), @Get(), @Session() decorator
- Services inject PrismaService via constructor
- All queries filter by organizationId from session
- Modules registered in app.module.ts imports array
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard API module with KPI, funnel, activity, agenda, and health endpoints</name>
  <files>
    apps/api/src/modules/dashboard/dashboard.module.ts,
    apps/api/src/modules/dashboard/dashboard.controller.ts,
    apps/api/src/modules/dashboard/dashboard.service.ts,
    apps/api/src/app.module.ts
  </files>
  <action>
    Create apps/api/src/modules/dashboard/ directory with:

    **dashboard.service.ts** — DashboardService with PrismaService injection:
    - `getKpis(orgId: string)`: Returns { leadsToday: number, appointmentsToday: number, conversionRate: number, revenuePipeline: number }. leadsToday = count of leads created today. appointmentsToday = count of appointments with startTime today. conversionRate = (leads with stage 'agendado'|'confirmado'|'atendido' / total leads) * 100 last 30 days. revenuePipeline = count of leads in 'qualificado'|'agendado' stages (placeholder for actual revenue).
    - `getFunnel(orgId: string)`: Returns array of { stage: string, count: number } for each pipeline stage, last 30 days. Query leads grouped by stage with count.
    - `getActivity(orgId: string, limit = 20)`: Returns recent LeadTimeline entries joined with lead name, ordered by createdAt desc. Shape: { id, leadName, eventType, fromStage, toStage, metadata, createdAt }.
    - `getAgenda(orgId: string)`: Returns today's appointments with lead name, professional, procedure, startTime, status. Query appointments where startTime between start and end of today, include lead relation.
    - `getAgentHealth(orgId: string)`: Returns { connected: boolean, messagesPerHour: number, errors: number, latency: number }. connected = any WhatsappInstance with status 'open'. messagesPerHour = count of conversations with lastMessageAt in last hour. errors = 0 (placeholder). latency = 0 (placeholder).

    **dashboard.controller.ts** — @Controller("dashboard"):
    - GET / → getKpis (calls service.getKpis with orgId from session)
    - GET /funnel → getFunnel
    - GET /activity → getActivity (accepts ?limit query param)
    - GET /agenda → getAgenda
    - GET /health → getAgentHealth
    All routes use @Session() to extract organizationId. Apply RBAC guard (owner, admin, manager).

    **dashboard.module.ts** — imports PrismaModule (or however PrismaService is provided), exports DashboardService.

    **app.module.ts** — add DashboardModule to imports array.

    All text responses in PT-BR where applicable. Use same patterns as LeadsController for auth/session handling.
  </action>
  <verify>
    <automated>cd "/Users/softsales/Documents/SEVYN LABS/CODE/Cliniq" && pnpm --filter @cliniq/api build 2>&1 | tail -5</automated>
  </verify>
  <done>5 dashboard endpoints return structured JSON. API builds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard frontend — KPI cards, funnel, timeline, agenda, health, sidebar nav</name>
  <files>
    apps/web/lib/types/dashboard.ts,
    apps/web/app/dashboard/components/kpi-cards.tsx,
    apps/web/app/dashboard/components/conversion-funnel.tsx,
    apps/web/app/dashboard/components/activity-timeline.tsx,
    apps/web/app/dashboard/components/todays-agenda.tsx,
    apps/web/app/dashboard/components/agent-health.tsx,
    apps/web/app/dashboard/page.tsx,
    apps/web/app/dashboard/layout.tsx
  </files>
  <action>
    **apps/web/lib/types/dashboard.ts** — TypeScript interfaces:
    ```typescript
    export interface DashboardKpis { leadsToday: number; appointmentsToday: number; conversionRate: number; revenuePipeline: number; }
    export interface FunnelStage { stage: string; count: number; }
    export interface ActivityItem { id: string; leadName: string; eventType: string; fromStage: string | null; toStage: string | null; metadata: Record<string, unknown>; createdAt: string; }
    export interface AgendaItem { id: string; leadName: string; professionalName: string; procedureName: string; startTime: string; status: string; }
    export interface AgentHealth { connected: boolean; messagesPerHour: number; errors: number; latency: number; }
    ```

    **kpi-cards.tsx** — "use client" component. Fetches from /api/dashboard on mount using apiClient.get. Renders 4 cards in responsive grid (grid-cols-1 sm:grid-cols-2 lg:grid-cols-4). Each card: rounded-xl bg-white p-6 shadow-sm ring-1 ring-gray-200. Shows label (text-sm text-gray-500), value (font-heading text-3xl font-bold), and small description. Cards: "Leads Hoje" with count, "Agendamentos Hoje" with count, "Taxa de Conversao" with percentage + %, "Pipeline" with count. Add subtle color accent: teal-600 for positive indicators. Loading skeleton with animate-pulse while fetching.

    **conversion-funnel.tsx** — "use client". Fetches /api/dashboard/funnel. Renders horizontal bar chart using pure CSS (no chart library needed for MVP). Each stage as a row: stage label (PT-BR: Novo, Qualificado, Agendado, Confirmado, Atendido, Perdido), bar width proportional to max count, count number. Teal gradient for active stages, gray for Perdido. Title: "Funil de Conversao — Ultimos 30 dias".

    **activity-timeline.tsx** — "use client". Fetches /api/dashboard/activity?limit=10. Renders vertical timeline with dots and lines. Each item: lead name bold, event description (e.g., "Moveu de Novo para Qualificado"), relative time (use Intl.RelativeTimeFormat). Scrollable max-h-80 overflow-y-auto.

    **todays-agenda.tsx** — "use client". Fetches /api/dashboard/agenda. Lists appointments in a card: time (HH:mm), lead name, professional, procedure, status badge (confirmado = green, pendente = yellow, cancelado = red). Empty state: "Nenhum agendamento para hoje".

    **agent-health.tsx** — "use client". Fetches /api/dashboard/health. Small card showing: connection dot (green if connected, red if not) with "WhatsApp Conectado"/"Desconectado", messages/hour stat, error count, latency. Compact inline layout.

    **page.tsx** — Replace placeholder content. Import all 5 components. Layout: KpiCards full width at top, then 2-column grid (lg:grid-cols-3) with ConversionFunnel (col-span-2) and AgentHealth (col-span-1), then 2-column grid with ActivityTimeline and TodaysAgenda. All responsive — stacks to single column on mobile. Title: "Dashboard" with subtitle "Visao geral da sua clinica".

    **layout.tsx** — Update sidebar nav to include:
    - Dashboard (icon: chart-bar, href: /dashboard) — active state with bg-teal-50 text-teal-700
    - Conversas (icon: chat-bubble, href: /dashboard/conversations)
    - Leads (icon: users, href: /dashboard/crm)
    - Calendario (icon: calendar, href: /dashboard/calendar)
    - Configuracoes (icon: cog, href: /dashboard/settings)
    Add clinic selector dropdown at top of sidebar below logo. Use authClient.organization.list() to get orgs, authClient.organization.setActive() on select. Show current org name. Mobile sidebar already works (hamburger menu exists). All nav items use simple SVG icons inline (no icon library needed). Active route detection via usePathname().
  </action>
  <verify>
    <automated>cd "/Users/softsales/Documents/SEVYN LABS/CODE/Cliniq" && pnpm --filter @cliniq/web build 2>&1 | tail -10</automated>
  </verify>
  <done>Dashboard page shows 5 live data sections (KPIs, funnel, timeline, agenda, health). Sidebar has full navigation with clinic selector. Layout is mobile-responsive (single column on mobile, multi-column on desktop). All text in PT-BR.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @cliniq/api build` passes — dashboard module compiles
2. `pnpm --filter @cliniq/web build` passes — all components compile
3. Dashboard page at /dashboard renders KPI cards, funnel, timeline, agenda, health sections
4. Sidebar shows navigation links: Dashboard, Conversas, Leads, Calendario, Configuracoes
5. On mobile viewport, layout stacks to single column without horizontal scroll
</verification>

<success_criteria>
- GET /api/dashboard returns KPI data structure
- GET /api/dashboard/funnel returns stage counts
- GET /api/dashboard/activity returns timeline items
- GET /api/dashboard/agenda returns today's appointments
- GET /api/dashboard/health returns agent connection status
- Dashboard page renders all 5 sections with loading states
- Sidebar has 5 nav links + clinic selector
- Mobile-responsive: no horizontal scroll on 375px viewport
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-settings/04-01-SUMMARY.md`
</output>
